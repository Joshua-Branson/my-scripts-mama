if textLabel then
	textLabel.Text = "Destroying rayfield in 2.9 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 2.8 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 2.7 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 2.6 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying rayfield in 2.5 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying rayfield in 2.4 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying rayfield in 2.3 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying rayfield in 2.2 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying rayfield in 2.1 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying rayfield in 2 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 1.9 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 1.8 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 1.7 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 1.6 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 1.5 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 1.4 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 1.3 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 1.2 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 1.1 Seconds."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 1 Second."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 0.9 Second."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 0.8 Second."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 0.7 Second."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 0.6 Second."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 0.5 Second."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 0.4 Second."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 0.3 Second."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 0.2 Second."
    task.wait(0.1)
    textLabel.Text = "Destroying Rayfield in 0.1 Second."
    task.wait(0.1)
    task.wait(1)
    textLabel.Text = "Now destroying Rayfield."
    task.wait(0.2)
else
    task.wait(3)
end

Rayfield:Destroy()
    end
end
